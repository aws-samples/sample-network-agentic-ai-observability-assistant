# Runtime dependencies for the Network Observability multi-agent app.
#
# This is the single dependency file used for BOTH local development
# (`pip install -r requirements.txt`) and AgentCore deployment, so the versions
# here are exactly what runs locally and in the cloud.
#
# Top-level packages are pinned to versions validated against a security scan
# (grype). Their transitive dependencies resolve to the latest compatible
# releases at install time. Bump these as new versions are released.
strands-agents==1.44.0
bedrock-agentcore==1.19.0
boto3==1.43.34
python-dotenv==1.2.2

# The `agentcore` CLI (configure / deploy / status / destroy).
bedrock-agentcore-starter-toolkit==0.3.11

# Tests
pytest==9.1.1
pytest-asyncio==1.3.0
